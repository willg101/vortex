This file is nested within two or more repositories, so Vortex will treat it as a member of the
outermost repository when setting breakpoints.
<br>
<br>
To explicitly specify a codebase for this file, you may create a <code>.vortex-codebase</code> file
that contains a name for the codebase. Vortex will use this file instead of repositories to
determine codebase boundaries.
